# EcommerceWebsite
A full-stack E-commerce application designed to manage product inventory.  Frontend: React (Vite) + Axios for handling dynamic forms and UI state.  Backend: Spring Boot (Java) + Spring Data JPA/Hibernate for exposing RESTful APIs and handling multipart file (image) uploads.  Database: PostgreSQL for robust relational data storage.
